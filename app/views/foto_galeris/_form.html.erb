<%= simple_form_for @foto_galeri do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :judul %>
    <%= f.input :keterangan %>
    <%= f.input :foto, as: :file %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
